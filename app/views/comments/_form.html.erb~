<% start_form_tag :action => 'add_comment' %>
  <%= text_field 'comment', 'comment' %>

  <%= submit_tag 'Add Comment' %>

  <%= hidden_field 'commentable', 'commentable', :value => @university.type %>
  <%= hidden_field 'commentable', 'commentable_id', :value => @university.id %>
<% end_form_tag %>
