<div class="span4">
		<div class="feature-box">
			<div class="box-content">
				<h4>Filtern</h4><p>
				<div class="form-group no-padding text-left">
					<%= search_form_for @searc,  url: search_advancedSearch_path do |g| %>
					<%= g.label :name_cont, "Name:" %>
					<%= g.text_field :name_or_address_city_name_cont, :class => "span3" %>
					# <%= g.label :university.avg_evaluation_gtec, "Rating over" %>
					# <%= g.text_field :university.avg_evaluation_gtec, :class => "span3" %>					
					<%= check_box_tag("q[hs_type_in][]", "FH") %>FH
					<p><%= check_box_tag("q[hs_type_in][]", "Uni") %>Uni</p>
					<div class="actions"><%= g.submit "Search" %></div>
					<% end %>
				</div>
			</div>
		</div>
</div>

<div class="row">
		<div class="span8">
			<h1>Liste</h1>

			
			<table id="quickList" class="table table-striped table-bordered">
			  <tr>
			    <th>Hochschule</th>
			    <th>Ort</th>
			    <th>Rating</th>
			  </tr><tbody id="innerlist">
			  <% @university.each do |university| %>
				<tr> 
					<td><%= link_to(university.name, university) %></td>
					<td><%= university.address.city.name %></td>
					<td><%= university.avg_evaluation %></td>
				</tr>
			  <% end %></tbody>
			</table>

		</div>
</div>
