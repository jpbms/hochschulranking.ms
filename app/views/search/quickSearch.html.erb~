<% content_for :javascript_includes do %>
  <%= javascript_include_tag 'jquery.raty.js','letsrate.js'%>
<% end %>
<div class="span4">
		<div class="feature-box">
			<div class="box-content">
				<h4>Filtern</h4><p>
				<div class="form-group no-padding text-left">
					<%= search_form_for @search,  url: search_quickSearch_path do |g| %>
					<%= g.label :name_cont, "Hochschule:" %>
					<%= g.text_field :name_or_address_city_name_cont, :class => "span3" %>
					<%= g.label :subjects_name_cont, "Studiengang:" %>
					<%= g.text_field :subject_subject_types_subject_name_cont, :class => "span3" %>
					<%= g.label :state_id_gteq, "Rating over" %>
					<%= g.text_field :state_id_gteq, :class => "span3" %>					
					<%= check_box_tag("q[hs_type_in][]", "FH") %>FH<br>
					<%= check_box_tag("q[hs_type_in][]", "Uni") %>Uni<br>
					<%= check_box_tag("q[campus_eq_any][]", "true") %>Campus
					<div class="actions"><%= g.submit "Search" %></div>
					<% end %>
				</div>
			</div>
		</div>
</div>

<div class="row">
		<div class="span8">
			<h1>Liste</h1>

			
			<table id="quickList" class="table table-striped table-bordered">
			  

				<tr>
				  <th><%= sort_link(@search, :name, "Hochschule") %></th>
				  <th><%= sort_link(@search, :address_city_name, "Ort") %></th>
				  <th>Rating</th>
				</tr>

			  <tbody id="innerlist">
			  <% @universities.each do |university| %>
				<tr> 
					<td><%= link_to(university.name, university) %></td>
					<td><%= university.address.city.name %></td>
					<%= javascript_include_tag 'application'%>           
					    <%= yield :javascript_includes %>
					<td><%= rating_for university, "allgemein", :star => 5%></td>
				</tr>
			  <% end %></tbody>
			</table>

		</div>
</div>

